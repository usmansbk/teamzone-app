import { Button, Container, Typography } from "@mui/material";

export default function Pages() {
  return (
    <Container fixed>
      <Typography variant="h1">Teamzone</Typography>
      <Typography variant="subtitle1">Clock app for remote teams</Typography>
      <Button>Hello</Button>
    </Container>
  );
}
